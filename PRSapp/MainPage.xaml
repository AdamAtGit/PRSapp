<Page x:Class="PRSapp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PRSapp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:m="using:PRSapp.Models"
    xmlns:vm="using:PRSapp.ViewModels"
    xmlns:uc="using:PRSapp.Views.UsrControls"
    xmlns:converters="using:PRSapp.ViewModels.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <vm:TimeViewModel/>
    </Page.DataContext>

    <Grid>
<TextBlock Text="Remember to c if UnitTest(uwp) can also help analyze the view's Databound  props and commands"
           Width="Auto" Height="Auto"
           FontSize="25"
           TextWrapping="Wrap"
           HorizontalAlignment="Center"
           VerticalAlignment="Top"/>
    </Grid>
</Page>
